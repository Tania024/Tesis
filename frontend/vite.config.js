// frontend/vite.config.js
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import { VitePWA } from 'vite-plugin-pwa'

export default defineConfig({
  plugins: [
    react(),
    VitePWA({
      // ✅ CONFIGURACIÓN PARA DESARROLLO
      registerType: 'autoUpdate',
      devOptions: {
        enabled: true, // ✅ ¡ESTO ES CLAVE PARA DESARROLLO!
        type: 'module'
      },
      manifest: {
        name: 'Museo Pumapungo',
        short_name: 'MuseoPuma',
        description: 'Guía personalizada para visitar el Museo Pumapungo',
        theme_color: '#3b82f6',
        background_color: '#ffffff',
        display: 'standalone',
        scope: '/',
        start_url: '/',
        icons: [
          {
            src: '/pwa-192x192.png',
            sizes: '192x192',
            type: 'image/png',
            purpose: 'any maskable'
          },
          {
            src: '/pwa-512x512.png',
            sizes: '512x512',
            type: 'image/png',
            purpose: 'any maskable'
          }
        ]
      },
      // ✅ CONFIGURACIÓN PARA GENERAR SERVICE WORKER
      workbox: {
        globPatterns: ['**/*.{js,css,html,ico,png,svg,json}'],
        runtimeCaching: [
          {
            urlPattern: /^https:\/\/.*\/api\/.*$/,
            handler: 'NetworkFirst',
            options: {
              cacheName: 'api-cache',
              expiration: {
                maxEntries: 50,
                maxAgeSeconds: 60 * 60 * 24
              }
            }
          }
        ]
      }
    })
  ],
  base: './'
})